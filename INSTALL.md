instantiate pgsql db rlsepp with user role, perms granted
run fetch_tickers
  base table instantiated by PGAdapter

post app sql updates:
psql rlsepp < librlsepp/config/sso_stuff.sql

populate transferfees table
populate walletstatus table


